<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <style>
        label {
            margin: 0.5em;
        }


        .section {
            margin: 1em;
            border: 1px solid rgb(61, 59, 59);
            padding: 20px;
            margin: 50px auto;
            max-width: 500px;
            border-radius: 5px;
            ;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            height: 100%;
            background-color: #fff;
            /*padding-right: 40px;*/
        }

        .section1 {
            margin: 1em;
            background-color: aqua;
            height: 300px;
            width: 60%;

        }


        .signup {
            display: flex;
            align-items: center;
            justify-content: center;


        }

        .form-group {

            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
        }

        .form-group input {
            padding-top: 10px;
            padding-bottom: 10px;

            width: 100%;
            border: 1px solid #c3c0c0;
            border-radius: 3px;
        }


        /* Add this CSS to your stylesheet */
        #button:hover {
            background-color: #0056b3;
        }

        .text {
            text-align: center;
        }

        #button {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 8px;
            color: indigo-600;
            border-radius: 5px;
            width: 100%;
            border: none;
            font-weight: bold;
            height: 50px;

            border-radius: 0.5rem;
            background-color: #4361ee;
            /* Indigo 600 */
            font-size: 1rem;
            /* Assuming this is what "text-sm" translates to */
            font-weight: 600;
            /* Assuming this is what "font-semibold" translates to */
            line-height: 1.75rem;
            /* Assuming this is what "leading-6" translates to */
            color: #fff;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.08);
            transition: background-color 0.2s ease-in-out;
        }

        #button:hover {
            background-color: #3f37c9;
            /* Indigo 500 */
        }

        #button:focus-visible {
            outline: 2px solid transparent;
            outline-offset: 2px;
            outline-color: #674cd1;
            /* Indigo 600 */
        }

        .home {
            border: none;
            display: flex;
            margin-bottom: 6px;
            ;
            justify-content: center;
            align-items: center;
            background-color: #4361ee;
            text-decoration: none;
            color: white;
            height: 40px;
            padding: 5px 5px 5px 5px;
            border-radius: 0.5rem;
            font-size: 1rem;
            /* Assuming this is what "text-sm" translates to */
            font-weight: 600;
            /* Assuming this is what "font-semibold" translates to */
            line-height: 1.75rem;
            /* Assuming this is what "leading-6" translates to */
            color: #fff;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.08);
            transition: background-color 0.2s ease-in-out;
        }

        .buttons {
            margin: 20px;
        }
    </style>
</head>

<body>
    <div class="signup">
        <div class='section'>
            <form id='sign-up' method="POST" action="/user">
                <h1 id="text" className="text-3xl font-bold ">Sign Up</h1>
                <p className="text-gray-500 dark:text-gray-400">Enter your email and password below to create your
                    account</p>
                <div class="form-group">
                    <label>Name</label>
                    <input type="text" name="name" placeholder="John Doe" />
                </div>
                <div class="form-group">
                    <label>Email</label>
                    <input type="email" name="email" type required="email" placeholder="m@example.com" />
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input id="password1" type="password" name="password" placeholder="Password" />
                </div>
                <div>
                    <p id="toggle1" onclick="showPassword1()" style="cursor: pointer ; color:black; font-weight:500;">
                        Show Password</p>
                </div>
                <div class="buttons">
                    <input type='submit' id="button">

                </div>
                <p>Already have an account? <a href="#" onclick="toggleForms()" style=" font-weight: 500;">Login
                        here</a></p>
                <div>
                    <a class="home" id="button1" href="#">Back to Home</a>
                </div>
            </form>
        </div>
    </div>
    <!--
    <h1>Sign up</h1>
    <form method="POST" action="/user">
        <label>Name</label>
        <input type="text" name="name" placeholder="John Doe" />
        <label>Email</label>
        <input type="email" name="email" placeholder="Email" />
        <label>Password</label>
        <input type="password" name="password" placeholder="Password" />
        <button type="submit">Signup</button>
    </form>-->
</body>

<script>function showPassword1() {
        var passwordField = document.getElementById('password1')
        if (passwordField.type === "password") {
            passwordField.type = "text"
        } else {
            passwordField.type = "password"
        }

    }</script>




</html>